# T-011 Report: Variance Check in Diagnosis

## What was done
I reviewed the existing variance check logic in `src/session/diagnosis.ts` which prevents giving directional advice (like "rushing" or "dragging") to a drummer who is simply scattered. This logic relies on evaluating `offsetStdDevMs` against a threshold.

## Mutation Testing
To verify the test suite adequately covers this logic, I mutated the threshold check:
- **Mutation:** `if (offsetStdDevMs > INCONSISTENT_SPREAD_MS)` -> `if (offsetStdDevMs >= INCONSISTENT_SPREAD_MS)`
  - **Result:** Survived. The tests do not assert the exact boundary condition where standard deviation equals `INCONSISTENT_SPREAD_MS`. (This is acceptable, but worth noting).
- **Mutation:** `if (!topRule || isDirectionalRule)` -> `if (!topRule && isDirectionalRule)`
  - **Result:** Caught. `src/session/__tests__/diagnosis.test.ts (R-T1)` failed, expecting "You're inconsistent." but receiving a directional error ("You're rushing...").

## Build and Tests
The build is green and tests pass reliably, taking around ~3-4 seconds. No further issues exist in `diagnosis.ts` or its corresponding tests.
