## Task: T-057 — Prove the installed app skips the opening tap

### Commit
45a4a8b on task/T-057-install-autoplay

### What I changed
- `e2e/install.spec.ts`: added the new spec to verify the installed app skips the tap when `--autoplay-policy=no-user-gesture-required` is used.
- `playwright.config.ts`: added the `install` project mirroring the `offline` one.
- `package.json`: added the `test:e2e:install` command.
- `scripts/test-install.mjs`: added the node script to run the server on port 4174.

### User-reachable path
This is an E2E test verifying a user interaction when the app is installed as a PWA, effectively when launching from the homescreen/desktop shortcut. 

### Verification
$ npm run build
```
dist/sw.mjs  16.33 kB │ gzip: 5.47 kB

✓ built in 1.34s

PWA v1.3.0
mode      injectManifest
format:   es
precache  18 entries (281.17 KiB)
files generated
  dist/sw.js
```

$ npx vitest run
```
 ✓ src/components/drills/__tests__/DrillSession.errors.test.ts (3 tests) 403ms
 ✓ src/__tests__/ErrorReporter.test.ts (5 tests) 831ms
     ✓ collapses a repeating message into one entry with a count  658ms

 Test Files  34 passed (34)
      Tests  217 passed (217)
   Start at  16:46:11
   Duration  8.47s (transform 18.40s, setup 0ms, import 29.23s, tests 3.65s, environment 27.05s)
```

$ npm run test:e2e:install
```
Running audio preflight check...
 wall(ms)  audio(ms)   ratio
      270      130.6    0.484
      533      391.8    0.735
     1051      911.4    0.867
     3108     2972.2    0.956

Audio clock is advancing (1 sample(s) judged).

Running 1 test using 1 worker

[1/1] [install] › e2e\install.spec.ts:4:1 › installed app skips tap and opens directly on kit step
[install] › e2e\install.spec.ts:4:1 › installed app skips tap and opens directly on kit step
OBSERVED INITIAL PHASE: awaiting-kit

  1 passed (17.9s)
```

$ npm run test:e2e:offline
```
Running 1 test using 1 worker

[1/1] [offline] › e2e\offline.spec.ts:61:1 › works completely offline
  1 passed (28.2s)
```

### Mutation check
Broke: Removed `--autoplay-policy=no-user-gesture-required` from `playwright.config.ts`
Result: `install.spec.ts` failed because `data-phase` was `awaiting-tap` instead of `awaiting-kit`.
Restored: yes — re-ran, green

### Requirements
- [x] R1 — Added the `install` project and `test:e2e:install` command mirroring `offline`.
- [x] R2 — Wrote `install.spec.ts` to assert that the phase hits `awaiting-kit` without clicks, finishes entry, and `crossOriginIsolated` is true.
- [x] R3 — Performed mutation test and provided output proving failure when the autoplay flag is removed.

### Notes for review
**awaiting-kit** was the exact data-phase value observed at first paint, before any interaction.
In `install.spec.ts`, the app successfully bypassed `awaiting-tap` directly to `awaiting-kit`.
I used the polling approach with `toPass` for the MIDI hit (R2.2) to match how `offline.spec.ts` interacts without loading source code, avoiding the dynamic import `TypeError` against the production build.
