## Task: T-039 — The hi-hat foot has two representations and only one is authoritative

### Commit
07ede0b on task/T-039-hihat-foot

### What I changed
- src/audio/midi.ts: Added deduplication logic to _handleNoteOn and the CC#4 handler to collapse note 44 and CC#4 chicks occurring within 100ms of each other.
- src/audio/__tests__/midi.test.ts: Added R1 test to prove double-counting occurred, and now proves it deduplicates.
- e2e/drill-audit.spec.ts: Extended audit harness to send both CC#4 and note 44 on a chick (R4).

### User-reachable path
Bootcamp -> "Hi-Hat Independence" (or any drill that uses hi-hat foot chicks).

### Verification
$ npm run build
? built in 429ms (dist/sw.js generated in 2.20s)

$ npx vitest run
Test Files  28 passed (28)
     Tests  173 passed (173)

$ npx playwright test --project=product e2e/drill-audit.spec.ts
31 passed (2.3m)

### Mutation check
Broke: Removed deduplication in midi.ts (letting note 44 through untouched alongside CC#4).
Result: Audit fails due to double-counting 
umHits in drill-audit.spec.ts.
Restored: yes — re-ran, green.

### Requirements
- [x] R1 — Established truth with a test in midi.test.ts. Before the fix, hits had length 2 for a single chick when sending both CC#4 and note 44. After the fix, length is 1.
- [x] R2 — Decided midi.ts owns the collapse via deduplication. This catches duplicate signals closest to the hardware before they pollute the hit stream (preventing double-counting 
umHits in DrillRunner). It also preserves support for modules that only send note 44 without CC#4.
- [x] R3 — I did not change the zone ID. MIDI_NOTE.HI_HAT_CHICK remains 44. Deduplicating physical and synthesized signals maintains correct scoring.
- [x] R4 — Extended the audit harness to send d.hit(44) alongside d.cc(4) for chicks.
- [x] R5 — Audit passes with 30 row checks, all unchanged.

### Notes for review
We chose deduplication in midi.ts instead of ignoring note 44 entirely. "CC#4 is authoritative" could imply we should just drop hardware note 44 completely, but doing so would break hardware modules that only send note 44 and lack continuous pedal data. Deduplication supports both types of kits seamlessly.
