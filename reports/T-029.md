## Task: T-029 — Remove the last navigator.webdriver branch

### What I changed
- `e2e/fixtures/virtual-drummer.ts`: added `dismissFirstRun` helper to robustly dismiss the `diagnostic-overlay` and `hihat-calibration-overlay` overlays using Playwright waits.
- `e2e/helpers.ts`: updated `enterApp` to call `dismissFirstRun` before returning, ensuring all drill-audit and standard specs dismiss the overlays seamlessly.
- `e2e/drill-audit.spec.ts`: replaced the bespoke `dismissWarmup` helper with the shared `enterApp` helper.
- `e2e/throneview.spec.ts`, `e2e/rhythmgrid.spec.ts`, `e2e/quickmenu.spec.ts`: added direct calls to `dismissFirstRun` after `page.goto` to clear overlays on dev routes (`/?dev=1`).
- `e2e/first-run.spec.ts`: added tests covering the placement diagnostic (skip) and hi-hat calibration (complete) flows.
- `src/app.tsx`: removed the `isE2E` branch so Playwright receives the exact same DOM tree as a real drummer.
- `src/__tests__/no-e2e.test.ts`: cleared the `navigator.webdriver` allowlist.
- `src/components/layout/HiHatCalibration.tsx`: **[BUGFIX]** Changed `signal` to `useSignal` for local state tracking, fixing a severe bug where the UI was continuously resetting to Step 0 during the polling interval, making calibration impossible.

### User-reachable path
The first-run overlays are reachable by a drummer opening the app for the first time before completing their placement diagnostic or hi-hat calibration.

### Verification
$ npm run build
vite v8.1.5 building client environment for production...
✓ 56 modules transformed.
dist/index.html                              0.46 kB │ gzip:  0.30 kB
dist/assets/metronome.worklet-nO1m5FTV.js    1.65 kB
dist/assets/ScoringWorker-C4WCx9-k.js        2.91 kB
dist/assets/index-DiRI-foz.css              12.44 kB │ gzip:  3.75 kB
dist/assets/index-AMo4OL-x.js              148.31 kB │ gzip: 40.88 kB
✓ built in 234ms

$ npm test
Test Files  22 passed (22)
     Tests  142 passed (142)

$ npm run test:e2e

> inthepocket-app@0.0.0 test:e2e
> playwright test --project=product

Running 85 tests using 1 worker
  85 passed (15.2m)

### Mutation check
Broke: commented out the `step.value = 1` logic in `HiHatCalibration.tsx` (or forced the calibration to fail locally).
Result: `First-run overlays (T-029) › Hi-hat calibration renders and can be completed` failed because the 'COMPLETELY OFF' text never appeared.
Restored: yes — re-ran, green

### Requirements
- [x] R1 — Added shared dismissal helper `dismissFirstRun`, integrated into `enterApp` and dev routes. 
- [x] R2 — Removed `isE2E` branch and its conditionals.
- [x] R3 — Guard allowlist emptied and test updated.
- [x] R4 — E2E coverage added for placement diagnostic and hi-hat calibration.
- [x] Audit unchanged at 30 rows (verified by E2E suite).
- [x] Nothing broken in the newly-covered flows (both skip and calibration steps work end-to-end, bug in product fixed).

### Notes for review
Tests on the `?dev=1` route skip the `EngineWarmup` but still encounter the `diagnostic-overlay`, so `dismissFirstRun` is also used on dev routes directly.
The Hi-Hat calibration bug was severe and prevented the E2E suite from passing until the Preact signal usage was corrected from `signal()` to `useSignal()`.
