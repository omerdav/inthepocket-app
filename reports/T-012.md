# T-012 Report: Hardware Blocking Logic

## What was done
I reviewed the existing hardware-blocking capability checks in `src/session/hardware.ts`, which verifies if the connected e-kit has the minimum viable zones required to perform a specific drill (e.g. checking if a kit lacks a `snare-rim`).

## Mutation Testing
To verify the test suite adequately covers this logic, I mutated the hardware check:
- **Mutation:** `if (noteMap && noteMap[zone] === null)` -> `if (noteMap && noteMap[zone] === undefined)`
  - **Result:** Caught. `src/session/__tests__/hardware.test.ts` immediately failed because `{ 'snare-rim': null }` no longer triggered the missing zone check, resulting in `ok` being `true` instead of `false`.

## Build and Tests
The build is green and tests pass reliably. The capability check logic correctly intercepts drills that the connected hardware cannot support.
